spring.application.name=proyecto_backend
server.port=8096


spring.datasource.url=jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
jwt.secret=dagjrkwbgiawnfjnewaogmio32nognemakngokn24jagn4kljnrag4234rg252fdafwefdsaDSAdsada
# Configuración para Gmail
spring.mail.host=smtp.gmail.com
spring.mail.port=587

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Google Maps APIs
google.maps.api.key=TU_API_KEY_AQUI
google.maps.geocoding.url=https://maps.googleapis.com/maps/api/geocode/json
google.maps.places.url=https://maps.googleapis.com/maps/api/place/autocomplete/json

# Email (corregir)
spring.mail.username=${SPRING_MAIL_USERNAME}
spring.mail.password=${SPRING_MAIL_PASSWORD}